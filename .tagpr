[tagpr]
	vPrefix = true
	# release = false is required: GitHub Release creation is handled by
	# the publish job in release.yml. tagpr must not create it.
	release = false
	changelog = true
	versionFile = action/setup-papion/action.yml;regexp=\bdefault:\s+([0-9]+\.[0-9]+\.[0-9]+)
